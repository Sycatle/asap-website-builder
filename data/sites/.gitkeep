# Static Site Projections

This directory contains generated JSON projections of published portfolios.

Each file is named `{slug}.json` and contains the complete portfolio data needed for rendering.

Example: `johndoe.json`

These files are generated by the projections module and consumed by the frontend for fast, static serving.
