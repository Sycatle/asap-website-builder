# Local override: host port 5432 is taken by another project's Postgres,
# so we don't expose asap-postgres on the host. Internal Docker network is
# all we need for migrations + sqlx prepare.
services:
  postgres:
    ports: !reset []
