---
import type { Element, Website } from '@asap/shared';
import SectionsWrapper from './SectionsWrapper';

interface Props {
  sections: Element[];
  website?: Website;
}

const { sections, website } = Astro.props;
---

<SectionsWrapper sections={sections} website={website} client:only="react" />
